# LedgerOne Backend

FastAPI backend for the LedgerOne accounting system.

## Development

```bash
# Install dependencies
uv sync

# Run development server
uv run uvicorn src.api.main:app --reload

# Run tests
uv run pytest
```
